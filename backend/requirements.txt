# ExamPath backend — core dependencies
# The pipeline uses these for real-world HTML/PDF parsing and live fetching.
httpx>=0.27              # HttpScraper (live official pages); swap-in for urllib
beautifulsoup4>=4.12     # robust HTML parsing for messy notices
lxml>=5.2                # fast parser backend for BeautifulSoup
pydantic>=2.7            # typed dataset schema (app/models.py)

# The API + n8n webhook run on the Python standard library (app/api/server.py) —
# no extra install needed. Prefer FastAPI? Uncomment and use it instead:
# fastapi>=0.111
# uvicorn>=0.30

# LLM-based extraction for hard documents (optional; see pipeline/extract.py):
# anthropic>=0.34        # or: openai>=1.40

# Tests run on the standard library:  python -m unittest -v
